#include<stdio.h>

int main()
{
    printf("Hello world");
    int i,age;
    for(i=0;i<10;i++)
{
    printf("Enter the agre \n");
    scanf("%d",&age);   
     if(age>10){
        break;    //continue in the continue statment loop can run again again if condition 
        // otherwise it will come outside the loop 12

    }
    printf(" we have not come across any continue statement ");
     printf(" we have not come across any continue statement ");
      printf(" we have not come across any continue statement ");
       printf(" we have not come across any continue statement ");
       printf("aarsh is good boy");
}
return 0;
}